interface:
  display_name: "AUQ"
  short_description: "Ask through native input or the AUQ GUI fallback"
  default_prompt: "Use $auq to ask me a structured clarification with the available native or AUQ GUI path."
